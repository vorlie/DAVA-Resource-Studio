[package]
name = "resourcefs"
version = "0.1.0"
edition = "2021"
description = "Virtual filesystem that transparently packs/unpacks DVPL files."

[dependencies]
dvpl     = { path = "../dvpl" }
walkdir  = "2"
anyhow   = "1"

[dev-dependencies]
tempfile = "3"
