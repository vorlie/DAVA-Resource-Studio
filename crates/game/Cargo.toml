[package]
name = "game"
version = "0.1.0"
edition = "2021"
description = "WoT Blitz / Lesta Tanks installation detection."

[dependencies]
anyhow = "1"

[target.'cfg(windows)'.dependencies]
winreg = "0.52"

[dev-dependencies]
tempfile = "3"
